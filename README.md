# KratosDB

KratsDB is a document store database that I plan to build from scratch as I work through Database Internals by Alex Petrov. It is largely going to be focused mostly on the storage engine using LSM trees. The target is to match or even beat the write-read performance (will define this more deeply in future PRs) of RocksDB which also uses LSM trees. 

